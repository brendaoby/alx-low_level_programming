#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints a hexadecimal string
 * Return: Always 0 (Success)
 */
int main (void)
{
char b;

for (b = '0'; b <= '9'; b++)
{
	putchar(b);
}
for (b = 'a'; b <= 'f'; b++)
{
	putchar(c);
}
putchar('\n');
return (0);
}
