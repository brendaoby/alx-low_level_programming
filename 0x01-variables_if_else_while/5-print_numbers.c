#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints base 10 digit numbers from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;

for (i = '0'; i <= '9'; i++)
printf("%d\n", i);
return (0);
}
