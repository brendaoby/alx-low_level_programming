#include "main.h"

/**
 * print_most_numbers - Prints all digits, but 2 and 4
 * Return: 0
 */

void print_most_numbers(void)
{
	int b;

	for (b = 48; b < 58; b++)
	{
		if (b != 50 && b != 52)
		{
			_putchar(i);
		}
	}
	_putchar('\n');
}
